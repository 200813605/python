#!/usr/bin/env python

color = BLUE = '\033[94m'
count = 0
user = []
maxmessages = 50
for message in range (maxmessages):
    count += 1
    message = raw_input (" Plese type a message to Leila, Nasser")

   for i in range (0,maxmessages)
       if count % 2 == 0 :
            print "From Leila"
            user = 'Leila'
        else :
                print'From Nasser'
            user = 'Nasser'
    
        filename = "Email.txt"
        myfile = open(filename,"w")
            for number in range (1) :
                if user == 'Leila' :
                str (myfile.write(message)), color + 'From Leila'
                myfile.close()

        if message == "" :
            break
